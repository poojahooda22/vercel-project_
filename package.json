{
  "name": "vercel-clone",
  "private": true,
  "version": "1.0.0",
  "description": "A Vercel clone: upload, build, serve and screenshot static deployments.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "// packageManager": "Required by turbo >=2.10 — it refuses to resolve the workspace without it, since which package manager is in use determines how it reads the lockfile.",
  "packageManager": "npm@10.8.2",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "engines": {
    "node": ">=22"
  }
}
